name: Build Rider Accept Pro APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Install dependencies
        run: npm install

      # 🪄 असली गॉड मोड: यहीं पर तुम्हारी withAndroidAutomator.js चलेगी और वो 5 फाइलें बनेंगी!
      - name: 🪄 Generate Android Engine (Expo Prebuild)
        run: npx expo prebuild --platform android --clean

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🛠️ Build APK (Compiling Java Robot)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: 🚀 Upload APK (Download Link)
        uses: actions/upload-artifact@v4
        with:
          name: Rider-Accept-Pro-APK
          path: android/app/build/outputs/apk/release/app-release.apk
