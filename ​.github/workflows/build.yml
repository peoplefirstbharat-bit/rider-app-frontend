name: Build Rider Accept Pro APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📦 Install dependencies
        run: npm install

      - name: 🪄 Expo Prebuild (यहाँ जनरेट होंगी सारी 100 Android फाइलें और Manifest)
        run: npx expo prebuild --platform android --clean

      - name: ☕ Setup Java (Android के लिए)
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🛠️ Build APK (Release Version)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: 🚀 Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: Rider-Accept-Pro-APK
          path: android/app/build/outputs/apk/release/app-release.apk
